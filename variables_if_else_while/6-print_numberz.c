#include <stdio.h>

/**
 * main - characters upper and lower case
 *
 * Return: zero
 */

int main(void)
{
int oskar;
for (oskar = '0'; oskar <= '9'; oskar++)
putchar(oskar + 0);
putchar('\n');
return (0);
}
